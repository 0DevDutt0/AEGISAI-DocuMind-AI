# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.0.3

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.7
alembic==1.13.1

# Security
python-jose[cryptography]==3.3.0
passlib==1.7.4
# bcrypt 4.1+ removed the __about__ attribute passlib 1.7.4 reads for version
# detection, which breaks hashing. Pin to the last compatible release.
bcrypt==4.0.1
python-multipart==0.0.6

# AI/ML
# crewai 0.19.0 requires openai>=1.13.3,<2.0.0 (1.3.5 is below its floor).
openai==1.13.3
ollama==0.1.7
langchain==0.1.12
langgraph==0.0.25
crewai==0.19.0

# Caching
redis==5.0.1

# Graph database
neo4j==5.10.0

# Observability
# Keep the stable (1.x) and instrumentation (0.xxbN) packages on the same
# release line: 1.23.0 pairs with 0.44b0. Mismatched lines pin conflicting
# opentelemetry-api versions and break `pip install`.
opentelemetry-sdk==1.23.0
opentelemetry-exporter-otlp==1.23.0
opentelemetry-instrumentation-fastapi==0.44b0

# Utilities
python-dotenv==1.0.0
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.3

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.0

# Documentation
mkdocs==1.5.3
mkdocstrings[python]==0.24.0